# Leave empty to call /v1 through the Vite proxy (no CORS in local dev).
# Set this only if the UI talks to the API on another origin.
VITE_API_BASE_URL=

# Set to true to send every URL to the Down for Maintenance page.
# Restart `npm run dev` after changing this.
VITE_DOWNTIME=
